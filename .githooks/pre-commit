#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" != "main" ]; then
  echo "This repository is main-only. Commit on main unless the user explicitly instructs otherwise."
  exit 1
fi

exit 0
